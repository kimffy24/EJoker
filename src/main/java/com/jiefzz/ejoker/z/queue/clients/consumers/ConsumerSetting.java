package com.jiefzz.ejoker.z.queue.clients.consumers;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class ConsumerSetting {

	final static Logger logger = LoggerFactory.getLogger(ConsumerSetting.class);
	
	static {
		logger.debug("[{}] is unuse now!", ConsumerSetting.class.getName());
	}
}
